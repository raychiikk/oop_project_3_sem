# Лабораторна робота №1. Рефакторінг та моделювання з використанням UML
## Проект "Weather Vibes"

### 1. Опис проекту
**"Weather Vibes"** — це веб-застосунок для перегляду погоди, розроблений з використанням **React** та **Vanilla JS** для бізнес-логіки. Проект демонструє принципи об'єктно-орієнтованого програмування (ООП), патерни проєктування та чітке розділення на шари (UI, Logic, Data).

---

### 2. Виконання завдання 1a (Refactoring & Modeling)

#### 🔄 Аналіз та Рефакторинг (AS IS -> TO BE)
У ході лабораторної роботи було проведено аналіз архітектури та виконано рефакторинг коду для дотримання принципів **SOLID** (зокрема Open/Closed Principle) та **DRY**.

**Впроваджені патерни проєктування:**

1.  **Factory Method (Фабричний метод):**
    * *Проблема:* Клас `WeatherService` мав жорстку залежність від конкретних класів умов (`SunnyCondition`, `RainyCondition`). Додавання нової погоди вимагало зміни логіки сервісу.
    * *Рішення:* Створено клас `WeatherFactory`, який інкапсулює логіку створення об'єктів. Сервіс тепер звертається до фабрики, що робить систему гнучкою до розширення.

2.  **Singleton (Одинак):**
    * *Проблема:* Потенційна розсинхронізація списку улюблених міст при створенні кількох екземплярів менеджерів.
    * *Рішення:* Клас `CityManager` перероблено на Singleton. Це гарантує існування єдиного екземпляра менеджера міст у межах всього застосунку.

#### 📊 UML Діаграми
Для проекту було розроблено повний комплект діаграм, що знаходяться у папці `uml/`.

* **Class Diagram:** Структура класів, використання патернів Factory та Singleton.
* **Use Case Diagram:** Сценарії взаємодії користувача з системою.
* **Sequence Diagram:** Деталізація процесу оновлення погоди через Фабрику.
* **Activity Diagram:** Алгоритм розрахунку статистики погоди.
* **State Machine Diagram:** Переходи між екранами (Dashboard, Forecast, Cities).
* **Component Diagram:** Архітектура застосунку (розподіл на шари Presentation, Logic, Data).
* **Deployment Diagram:** Схема розгортання веб-застосунку.
* **Object Diagram:** Знімок стану системи (об'єкти в пам'яті) в конкретний момент часу.

#### ✅ Тестування
Реалізовано Unit-тести з використанням **Vitest**.
* Покриття тестів: Базова логіка, перевірка патернів (Singleton, Factory), коректність розрахунків.
* Запуск тестів: `npm test`

---

### 3. Додаткові завдання

* **1b (Modeling):** Розроблено концептуальні діаграми для зовнішнього сервісу.

---

### 4. Встановлення та запуск

1.  Клонуйте репозиторій:
    ```bash
    git clone [https://github.com/raychiikk/WeatherVibes-OOP.git](https://github.com/raychiikk/WeatherVibes-OOP.git)
    ```
2.  Перейдіть до каталогу проекту:
    ```bash
    cd WeatherVibes-OOP
    ```
3.  Встановіть залежності:
    ```bash
    npm install
    ```
4.  Запустіть проект у режимі розробки:
    ```bash
    npm start
    ```
5.  Запустіть тести:
    ```bash
    npm test
    ```

### 5. Технологічний стек
* **Frontend:** React 19
* **Styling:** Tailwind CSS 4
* **Testing:** Vitest
* **Charts:** Recharts
